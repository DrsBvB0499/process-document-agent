# Python packages for Document Agent
# Note: To render flowcharts as images, also install Mermaid CLI (via npm):
#   npm install -g @mermaid-js/mermaid-cli

anthropic>=0.40.0
python-docx>=1.1.0
Pillow>=10.0.0
python-dotenv>=1.0.0
openai>=1.12.0
tabulate>=0.9.0